{%- comment -%}
  Estelle Atelier - Icon Snippet
  Loads SVG icons from assets/ folder (Dawn pattern)

  Usage:
    {% render 'icon', icon: 'cart' %}
    {% render 'icon', icon: 'search', class: 'custom-class' %}

  Parameters:
    - icon: Icon filename without 'icon-' prefix (required)
    - class: Additional CSS classes (optional)
{%- endcomment -%}

{%- assign icon_file = icon | prepend: 'icon-' | append: '.svg' -%}

<div class="icon {{ class | default: '' }}">
  {{ icon_file | asset_url | file_img_tag: icon, 'svg icon' }}
</div>
